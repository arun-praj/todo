name: Test Frontend
on:
  push:
    branches: [develop]
  workflow_dispatch:
jobs:
  build-n-test:
    runs-on: self-hosted
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 18
      - name: Install dependencies
        continue-on-error: false
        run: npm ci
      - name: Run test
        run: npm run test
      
